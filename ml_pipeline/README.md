# ML Pipeline

Run the end-to-end training pipeline with:

```bash
python -m ml_pipeline.train_pipeline --data-version v1
```

Artifacts are stored under models/versions/<version_id> and models/current.
