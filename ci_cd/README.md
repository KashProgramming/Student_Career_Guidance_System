# CI/CD

GitHub Actions workflow lives in .github/workflows/mlops.yml and supports push, pull requests, scheduled runs, and manual triggers.

Steps include linting, dataset schema validation, training pipeline checks, and Docker builds.
Configure image pushing and deployment steps in the placeholders.
